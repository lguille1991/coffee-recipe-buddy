# Method Selection Confirmation Plan

- [ ] Update `src/app/methods/page.tsx` so tapping a brewing method no longer generates a recipe immediately.
- [ ] Split the current `selectMethod` behavior into two paths:
  - selection path: first tap only sets or clears the active method card
  - submission path: a separate continue action performs the existing `/api/generate-recipe` request and navigation to `/recipe`
- [ ] Keep selection state local to the page with an explicit selected recommendation object or selected method id, separate from the existing `selecting` loading state.
- [ ] Apply the new interaction to both recommended methods and expanded “Other methods” so every card behaves consistently.
- [ ] Preserve the current “Other methods” warning copy and expand/collapse behavior.
- [ ] Add selected-state styling to the active card:
  - stronger border or ring
  - selected indicator instead of the current chevron-only affordance
  - clearer pressed/focus treatment so the first click reads as “selected” rather than “committed”
- [ ] Allow tapping the already-selected card to clear the selection so users can back out before continuing.
- [ ] Add a sticky bottom CTA on the methods screen, aligned with the app’s existing fixed-footer pattern, with these rules:
  - disabled when no method is selected
  - label updates to include the chosen method name, for example `Continue with Hario V60`
  - shows the existing loading spinner while recipe generation is in flight
  - disables method cards while the request is in flight
- [ ] Keep the current API payload and recipe/session persistence behavior unchanged once the CTA is pressed:
  - read bean and target volume from `recipeSessionStorage`
  - post to `/api/generate-recipe`
  - store generated recipe
  - clear prior recipe-original, feedback round, and adjustment history
  - persist the selected method recommendation via `setSelectedMethod`
  - navigate to `/recipe`
- [ ] Hydrate the initial selected card from `recipeSessionStorage.getSelectedMethod()` when available, but never auto-submit; this preserves context if a user returns to `/methods`.
- [ ] Add button accessibility semantics for selection state, using an explicit selected/pressed indicator and preserving keyboard activation behavior.
- [ ] Remove or replace any iconography that still implies “tap once to navigate immediately.”
- [ ] Bump `package.json` version from `1.4.25` to `1.4.26` because this is a UX fix/refinement, not a new feature or breaking change.
- [ ] Verification plan:
  - run `npm test`
  - run `npm run lint`
  - manually verify recommended-method flow: first tap selects, second tap on same card deselects, CTA stays disabled until a selection exists, CTA generates recipe and routes correctly
  - manually verify “Other methods” flow behaves the same way
  - manually verify returning to `/methods` after a prior selection restores the highlight without auto-generating
  - manually verify loading state prevents double submits
- [ ] Assumptions and defaults:
  - no backend or schema changes are required
  - no new test infrastructure will be introduced for this UI-only change; verification stays with existing lint/test commands plus manual flow checks
  - the sticky CTA is the only confirmation control; no modal or bottom-sheet confirmation is added
  - suggested commit message: `Add explicit confirmation step for brew method selection`
